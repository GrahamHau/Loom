PORT=3000
DATA_DIR=./data
SESSION_SECRET=change-this-long-random-string
APP_USERNAME=graham
APP_PASSWORD=change-this-password

# OpenAI-compatible LLM settings are stored via the app settings page.
# Keep real API keys out of Git.
