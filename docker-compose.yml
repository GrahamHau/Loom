services:
  app:
    build: .
    container_name: loom
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env.production
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ../loom-seal:/home/ubuntu/apps/loom-seal:ro
    networks:
      - default
      - edge

networks:
  edge:
    external: true
    name: sub2api_sub2api-network
