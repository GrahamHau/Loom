# Where the "进入 LOOM" CTA points when the user is not logging in via the
# embedded form. In local dev this is the main LOOM Vite app; in production
# Codex will rewrite this to the deployed LOOM URL (or leave it as same-origin
# so the embedded login form handles everything).
VITE_LOOM_APP_URL=http://localhost:5173
